version: '3.2'
services:
    las:
        image: sconecuratedimages/services:las
        environment:
          - SCONE_LOG=7
          - SCONE_MODE=sim
        ports:
          - target: 18766
            published: 18766
            protocol: tcp
            mode: host
